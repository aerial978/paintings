<header class="navbar navbar-expand-lg bg-light p-4 mb-2">
  <div class="container">
    <a class="navbar-brand" href="{{ path('home.index') }}"><h3>Painting.</h3></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav m-auto fs-5">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{ path('home.index') }}">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('painting.index') }}">Artworks</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('blogpost.index') }}">News</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Portfolio
          </a>
          <ul class="dropdown-menu">
            {% for category in categoryNavbar() %}
								<li class="dropdown-item">
									<a href="{{ path('portfolio.category', {'slug': category.slug}) }}" class="text-decoration-none text-dark">{{ category.name }}</a>
								</li>
							{% endfor %}
          </ul>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('about') }}">About</a>
        </li>
      </ul>
      <a class="navbar-btn btn btn-lg btn-primary text-light text-end" href="#">Contact Me</a>
    </div>
  </div>
</header>
